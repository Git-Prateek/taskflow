[build]
  command   = "npm run build"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA routing — serve index.html for all routes so /dashboard etc. work after refresh
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
